name: Renovate Dependencies

on:
  schedule:
    # https://crontab.guru/#5_12_*_*_1
    - cron: "5 12 * * 1"
  workflow_dispatch:

jobs:
  sync:
    uses: curioswitch/go-build/.github/workflows/export-renovate.yaml@7489f72867246693fc1cc65560c68cdc860a1f8a # v0.5.2
    with:
      github_app_id: "1047503"
    secrets:
      github_app_key: ${{ secrets.CURIOSWITCH_APP_KEY }}
